{
  "installCommand": "pnpm --dir crm install --no-frozen-lockfile",
  "buildCommand": "pnpm --dir crm build",
  "outputDirectory": "crm/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}